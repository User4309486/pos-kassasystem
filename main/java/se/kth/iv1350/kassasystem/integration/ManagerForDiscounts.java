package main.java.se.kth.iv1350.kassasystem.integration;

import main.java.se.kth.iv1350.kassasystem.model.DTOForSale;

public class ManagerForDiscounts {
    public ManagerForDiscounts() {
    }

    /*
     * The discount is calculated based on the customer ID, product IDs, order total
     * and so on. The DTOForDiscount stores productIDs and customerID.
     * 
     * @return the discount amount
     */

    public double findDiscount(DTOForSale orderInfo, DTOForDiscount customer) {
        return 0;
    }
}
